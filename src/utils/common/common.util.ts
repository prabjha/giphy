export const maxNumber = (num1: number, num2: number): number => {
  if (num1 < num2) return num2;
  return num1;
};
export const minNumber = (num1: number, num2: number): number => {
  if (num1 < num2) return num1;
  return num2;
};
